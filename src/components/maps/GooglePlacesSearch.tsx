// This file is deprecated. Use FreeLocationSearch instead.
// Keeping for backwards compatibility - redirects to free alternative.

import FreeLocationSearch from './FreeLocationSearch';

// Re-export the free alternative with the same interface
export default FreeLocationSearch;
