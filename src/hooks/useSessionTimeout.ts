/**
 * useSessionTimeout — Disabled
 * 
 * Session timeout has been disabled. Users remain logged in until they
 * explicitly sign out. The hook is kept as a no-op to avoid breaking
 * existing imports.
 */
export function useSessionTimeout() {
  // No-op — session persists indefinitely
}
